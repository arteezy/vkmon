<!DOCTYPE html>
<html lang='en'>
  <head>
    <title>VKMon</title>
    <meta charset='utf-8'>
    <meta name='viewport' content='width=device-width, initial-scale=1, shrink-to-fit=no'>
    <meta http-equiv='x-ua-compatible' content='ie=edge'>
    <%= csrf_meta_tags %>
    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class='container'>
      <nav class='navbar navbar-toggleable-sm navbar-inverse bg-primary'>
        <button class='navbar-toggler navbar-toggler-right' type='button' data-toggle='collapse' data-target='#collapsed-navbar' aria-controls='collapsed-navbar' aria-expanded='false' aria-label='Toggle navigation'>
          <span class='navbar-toggler-icon'></span>
        </button>
        <%= link_to 'VKMon', root_path, class: 'navbar-brand' %>
        <div class='collapse navbar-collapse' id='collapsed-navbar'>
          <div class='navbar-nav mr-auto'>
            <%= link_to 'Watchers', watchers_path, class: 'nav-item nav-link' %>
            <%= link_to 'Status', status_path, class: 'nav-item nav-link' %>
          </div>
          <div class='navbar-nav'>
          <% if signed_in? %>
            <%= link_to current_user.email, new_password_path, class: 'nav-item nav-link' %>
            <%= link_to 'Sign out', sign_out_path, method: :delete, class: 'nav-item nav-link' %>
          <% else %>
            <%= link_to 'Sign in', sign_in_path, class: 'nav-item nav-link' %>
          <% end %>
          </div>
        </div>
      </nav>
    </div>

    <div class='container'>
      <% unless flash.blank? %>
        <div class='row'>
          <div class='col-sm-12'>
            <div id='flash'>
              <% flash.each do |key, value| %>
                <div class='flash alert alert-dismissible fade show alert-<%= key == 'notice' ? 'info' : 'danger' %>' role='alert'>
                  <button type='button' class='close' data-dismiss='alert' aria-label='Close'>
                    <span aria-hidden='true'>&times;</span>
                  </button>
                  <%= value %>
                </div>
              <% end %>
            </div>
          </div>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
